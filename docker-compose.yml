version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/app:/app/app
    restart: always

# MongoDB Atlas를 사용하므로 로컬 DB 컨테이너는 생략하거나 
# 테스트용으로 필요시 추가할 수 있습니다.
