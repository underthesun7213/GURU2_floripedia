# FastAPI & ASGI Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0        # 프로덕션 프로세스 매니저(무중단 reload). deploy/gunicorn.conf.py 참고

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# File Upload
python-multipart==0.0.12

# Data Validation & Settings — [email]: 스키마의 EmailStr이 email-validator 필요
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Database
motor==3.6.0
certifi>=2024.0.0

# Cache (인메모리 캐시 계층; 추후 Redis 교체 대비 추상화)
cachetools==7.0.5

# AI & ML  (신 SDK: 코드가 `from google import genai` / genai.Client 사용)
google-genai==1.65.0

# Firebase
firebase-admin==6.5.0

# Environment Variables
python-dotenv==1.0.1

# Image Processing
Pillow==10.4.0

# HTTP Client (for image search API) — google-genai가 >=0.28.1 요구
httpx==0.28.1

# Kew Gardens API (POWO plant data) — scripts/_legacy/etl_add_plants.py 전용
pykew==0.1.3
